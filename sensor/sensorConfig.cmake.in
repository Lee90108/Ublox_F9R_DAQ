# - Config file for the sensor package
# It defines the following variables
#  sensor_INCLUDE_DIRS - include directories for FooBar
#  sensor_LIBRARIES    - libraries to link against
#  sensor_EXECUTABLE   - the bar executable

set(sensor_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${sensor_DIR}/export_sensor.cmake")

# These are IMPORTED targets created by sensorTargets.cmake


